import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '6packs-Workout-Mobile',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
